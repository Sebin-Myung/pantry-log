import { Text, View } from "react-native";

export default function AllCategory() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}>
      <Text>전체</Text>
    </View>
  );
}
